version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/data/kritis.db
    volumes:
      - kritis-data:/data
    restart: unless-stopped

volumes:
  kritis-data:
    driver: local
